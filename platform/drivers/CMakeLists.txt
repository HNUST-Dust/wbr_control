if(CONFIG_WBR_CONTROL_RUNTIME_INIT_CAN)
  file(GLOB_RECURSE WBR_CONTROL_CAN_SOURCES CONFIGURE_DEPENDS
    ${CMAKE_CURRENT_SOURCE_DIR}/communication/can_dispatch.cpp
  )
  target_sources(wbr_platform PRIVATE ${WBR_CONTROL_CAN_SOURCES})
endif()

if(CONFIG_WBR_CONTROL_RUNTIME_INIT_USB)
  file(GLOB_RECURSE WBR_CONTROL_USB_SOURCES CONFIGURE_DEPENDS
    ${CMAKE_CURRENT_SOURCE_DIR}/communication/usb_session.cpp
  )
  target_sources(wbr_platform PRIVATE ${WBR_CONTROL_USB_SOURCES})
endif()

file(GLOB_RECURSE WBR_CONTROL_DEVICE_SOURCES CONFIGURE_DEPENDS
  ${CMAKE_CURRENT_SOURCE_DIR}/devices/*.c
  ${CMAKE_CURRENT_SOURCE_DIR}/devices/*.cc
  ${CMAKE_CURRENT_SOURCE_DIR}/devices/*.cpp
)

target_sources(wbr_platform PRIVATE ${WBR_CONTROL_DEVICE_SOURCES})
