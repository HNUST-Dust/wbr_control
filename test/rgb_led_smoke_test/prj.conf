# Text diagnostics are configured by ../common/rtt.conf. UART0 is VOFA-only.
CONFIG_GPIO=y

# Make unexpected kernel failures stop at the failure site instead of silently
# rebooting and making the LED sequence look like a reset problem.
CONFIG_REBOOT=n
